app-id: com.pojtinger.felix.hydrapp.example
runtime: org.freedesktop.Platform
runtime-version: "21.08"
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang
  - org.freedesktop.Sdk.Extension.ImageMagick
command: com.pojtinger.felix.hydrapp.example
finish-args:
  - --socket=session-bus
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: com.pojtinger.felix.hydrapp.example
    buildsystem: simple
    build-commands:
      - . /usr/lib/sdk/golang/enable.sh; . /usr/lib/sdk/ImageMagick/enable.sh; make PREFIX=/app
      - . /usr/lib/sdk/golang/enable.sh; . /usr/lib/sdk/ImageMagick/enable.sh; make install PREFIX=/app
      - appstream-util validate-relax com.pojtinger.felix.hydrapp.example.metainfo.xml
      - install -D -m 0644 com.pojtinger.felix.hydrapp.example.metainfo.xml /app/share/metainfo/com.pojtinger.felix.hydrapp.example.metainfo.xml
    build-options:
      build-args:
        - "--share=network"
    sources:
      - type: dir
        path: .
