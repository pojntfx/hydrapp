app-id: com.pojtinger.flatpakintegratedwebserver
runtime: org.freedesktop.Platform
runtime-version: "20.08"
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang
  - org.freedesktop.Sdk.Extension.ImageMagick
command: com.pojtinger.flatpakintegratedwebserver
finish-args:
  - --socket=session-bus
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: com.pojtinger.flatpakintegratedwebserver
    buildsystem: simple
    build-commands:
      - . /usr/lib/sdk/golang/enable.sh; . /usr/lib/sdk/ImageMagick/enable.sh; make PREFIX=/app
      - . /usr/lib/sdk/golang/enable.sh; . /usr/lib/sdk/ImageMagick/enable.sh; make install PREFIX=/app
      - appstream-util validate-relax com.pojtinger.flatpakintegratedwebserver.metainfo.xml
      - install -D -m 0644 com.pojtinger.flatpakintegratedwebserver.metainfo.xml /app/share/metainfo/com.pojtinger.flatpakintegratedwebserver.metainfo.xml
    build-options:
      build-args:
        - "--share=network"
    sources:
      - type: dir
        path: .
